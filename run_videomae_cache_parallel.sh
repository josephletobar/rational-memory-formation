#!/usr/bin/env bash
# VideoMAE was explicitly removed from the active workload.  This stub keeps
# detached legacy launchers from consuming the pod GPU if they are triggered.
echo "VideoMAE cache worker disabled by current project scope." >&2
exit 0
